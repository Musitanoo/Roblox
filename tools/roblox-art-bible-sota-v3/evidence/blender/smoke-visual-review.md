# Smoke visual review — non-authoritative

Automated build status: PASS. Evidence tier: `SMOKE_COMPLETE`.

The structured attestation is `evidence/blender/smoke-evidence.json`. It records
all build-report and image hashes; the generated image bytes remain in the
reproducible local `build/` directory and are not bundled in the release ZIP.

- 324/324 required smoke renders exist and match their recorded SHA-256.
- Six 30-instance stress renders exist.
- Near barricade views show distinct intact/damaged/critical geometry cues.
- Far enemy silhouettes remain visible at `640×360`.
- Territory massing remains distinguishable in the sampled stress views.

This review records an agent observation only. It is not provenance approval,
a sealed participant result, a territory selection, or production approval.
